Instructions:

1. Run the Python script with Python 3 like this:
    python3 sizer.py

2. This will run the Python script which will get a list of windows to resize which get passed the bash script one-by-one.
3. The bash script will resize each given window. Sizes and positions can be added to the script for any given window title.
